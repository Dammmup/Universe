# 🌌 Конструктор Реальности — Reality Constructor

## Суть проекта

Это не сайт. Это интерактивная философская вселенная — живой 3D-движок, в котором пользователь буквально проваливается сквозь слои реальности: от Большого Взрыва до нейронов собственного мозга.

Пользователь не читает — он проходит путь. Каждый уровень — это масштаб бытия со своими законами, факторами и визуальным языком.

---

## Стек технологий

* **React** — UI и компонентная архитектура
* **React Three Fiber (@react-three/fiber)** — 3D рендеринг на основе Three.js
* **Three.js** — геометрии, частицы, шейдеры, BufferGeometry
* **Zustand** — глобальный стейт (текущий stage, reversedFactors)
* **GSAP** — анимации камеры и переходов между стадиями
* **Framer Motion** — UI анимации (модалки, появление текста)
* **Tailwind CSS** — стилизация UI-слоя

---

## Архитектура стадий (Stages)

Навигация между стадиями осуществляется  **скроллом колёсика мыши** .

| Stage | Название                           | Компонент                                | Описание                                                                                                            |
| ----- | ------------------------------------------ | ------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| 0     | Сингулярность                 | `BigBang.jsx`                                   | Чёрный экран, одна точка. Клик → взрыв частиц, камера отлетает назад |
| 1     | Космос и Метафизика       | `Cosmos.jsx`                                    | 5000 звёзд, Земля, Луна, кометы, метафизические объекты                            |
| 2     | Природа и Стихии             | `Planet.jsx`(природная сторона) | Океаны, острова, тектоника, облака, фотосинтез                                        |
| 3     | Общество и Цивилизация | `Planet.jsx`(городская сторона) | Мегаполис, небоскрёбы, война, прогресс, смог                                            |
| 4     | Тело, эмоции, личность  | `HumanBody.jsx`                               | Анатомическая фигура, органы, области тела с факторами                                   |
| 5     | Микрокосмос / Сознание  | `MicroCosmos.jsx`                               | Клетка, ядро с ДНК, митохондрии, синапс                                   |
| 6     | Итог пути                          | `Finale.jsx`                               | Шесть масштабов одной нитью света, искры перевёрнутых факторов                                   |

---

## Структура файлов

```
Universe/
├── src/
│   ├── App.jsx                     # Рендер сцен, режиссёрская камера, UI Overlay
│   ├── store.js                    # Zustand store: stage (0-6), shift, reversedFactors
│   ├── data/
│   │   ├── factors.js              # Тексты факторов
│   │   ├── body.js                 # Области тела: зоны наведения, кадры, факторы
│   │   └── geography.js            # Города, горные цепи, леса
│   ├── lib/
│   │   ├── journey.js              # Опорные кадры камеры
│   │   ├── veilPresets.js          # Тайминги и палитра переходов между слоями
│   │   ├── anatomy.js              # Геометрия фигуры человека
│   │   ├── organs.js               # Геометрия внутренних органов
│   │   └── shaders/                # earth, aurora, life, veil
│   ├── components/
│   │   └── Onboarding.jsx          # Подсказка по управлению при первом запуске
│   └── scenes/
│       ├── BigBang.jsx             # Stage 0: Сингулярность и взрыв
│       ├── Cosmos.jsx              # Stage 1: Солнечная система
│       ├── Planet.jsx              # Stage 2-3: Планета (природа + общество)
│       ├── MicroCosmos.jsx         # Stage 4: Клетка и синапс
│       ├── HumanBody.jsx           # Stage 5: Тело, органы, области
│       ├── Finale.jsx              # Stage 6: Итог пути
│       ├── earth/                  # Глобус, природный и городской слои
│       └── effects/
│           ├── SceneVeil.jsx       # Вуаль перехода между слоями
│           ├── PostFX.jsx          # Свечение, виньетка, аберрация, зерно
│           └── FactorMarker.jsx    # Единая метка фактора для всех сцен
```

---

## Переходы между слоями

Слои — это разные сцены Three.js. Прямая подмена одной на другую читается как
монтажная склейка, поэтому каждый стык накрыт **вуалью**: полноэкранной заливкой
с радиальными полосами светового прыжка (`SceneVeil` + `lib/shaders/veil.js`).

Порядок один и тот же:

1. `beginShift(kind, commit)` — кадр начинает заливаться от центра к краям;
   камера в этот момент ускоряется внутрь кадра (вниз по масштабу) или
   отрывается назад (вверх).
2. `commitShift()` в середине — под непрозрачной заливкой меняется `stage`,
   новая сцена монтируется, камера ставится в точку въезда.
3. `endShift()` — заливка расходится, и кадр открывается **уже в движении**:
   камера едет к финальной точке, пока вуаль сходит.

Типы переходов (`VEIL_KIND` в `store.js`): `bang` (взрыв), `dive` (вход в
атмосферу), `matter` (проваливание в вещество), `flesh` (клетка → тело),
`origin` (клетка → итог), `ascend` / `collapse` (возврат наверх).

Человек стоит перед клеткой: масштаб убывает монотонно. Прежний порядок
«общество → клетка → человек» проваливал зрителя в микромир и возвращал
обратно к телу. Номера слоёв в коде названы — `HUMAN_STAGE` и `CELL_STAGE`
в `App.jsx`.

Два перехода вуалью не накрыты намеренно:

* **1→2** — сначала живой пролёт сквозь Солнечную систему к Земле, и только на
  контакте с атмосферой включается вспышка;
* **2↔3** — сцена одна и та же, планета сама доворачивается к другому полушарию.

Сцены предзагружаются в `requestIdleCallback`: иначе чанк грузится в момент
перехода и вместо стыковки зритель видит спиннер `Suspense`.

Порог колеса одинаковый на всех слоях. Отдельный высокий порог на микро-уровне
отдавал колесо зуму, но реальная мышь шлёт `deltaY` около 120 — из клетки нельзя
было уйти ни вперёд, ни назад, и при возврате с антропо-уровня зритель снова
застревал в микромире. Зум остался на `Ctrl` + колесо.

---

## Антропо-уровень: области тела

Фигура (`lib/anatomy.js`) собирается протяжкой эллиптических сечений вдоль
осевых кривых: талия, грудная клетка и сужение к запястью задаются профилем
радиусов. Рост ≈ 7.4 единицы, стопы на `y ≈ -3.7`, макушка на `y ≈ 3.84`.

Тело разбито на области (`data/body.js`): голова, грудь, живот, руки, ноги,
позвоночник. У каждой — своя зона наведения, свой кадр камеры и свой набор
факторов (3–5 на область).

* Сама оболочка тела не перехватывает лучи (`raycast={() => null}`): по
  непрерывной поверхности грудь от живота не отличить, прицел дают невидимые
  примитивы внутри.
* Пока область не выбрана, в кадре только названия областей. Два десятка
  подписей одновременно превращали фигуру в облако текста.
* Выбор области подводит камеру (`JourneyCamera` слушает `bodyRegion`) и
  раскрывает её факторы. Клик по пустому месту или «← Всё тело» возвращает
  общий план.
* Фигура не вращается сама: по областям надо прицеливаться, а уезжающая
  из-под курсора мишень делает это невозможным.

Органы (`lib/organs.js`) строятся вокруг локального нуля и ставятся на место
позицией меша: шейдер ткани считает складки по локальным координатам вершины, и
в координатах фигуры борозды коры выродились бы в рябь. Куда какой орган
встаёт — таблица `ORGAN_AT` в `HumanBody.jsx`.

---

## Интерактивные факторы

Каждая сцена содержит кликабельные 3D-объекты —  **факторы** . При клике открывается модальное окно с названием, описанием и кнопкой  **"Включить реверс"** .

Реверс меняет:

1. Название фактора (например: УСКОРЕНИЕ → ЗАМЕДЛЕНИЕ)
2. Визуальное поведение объекта в сцене (цвет, анимация, форма)

### Факторы по стадиям:

**Stage 1 — Космос:**

* `acceleration` — Ускорение / Замедление
* `gravity` — Тяжесть / Распад (орбитальные линии разлетаются)
* `sun` — Солнце / Угасание *(не реализовано)*
* `heating` — Нагревание / Замерзание *(не реализовано)*

**Stage 2 — Природа:**

* `tectonics` — Тектоника / Землетрясения
* `waterCycle` — Круговорот / Засуха
* `photosynthesis` — Фотосинтез / Увядание
* `interference` — Интерференция / Изоляция *(не реализовано)*

**Stage 3 — Общество:**

* `war` — Война / Мир
* `progress` — Прогресс / Стагнация
* `ecology` — Экология / Смог

**Stage 4 — Микрокосмос:**

* `dominance` — Доминанта / Рассеянность
* `abstraction` — Абстракция / Буквальность
* `mutation` — Мутация / Стагнация *(не реализовано)*

---

## Глобальный стейт (store.js)

```js
{
  stage: 0,              // Текущая стадия (0-6)
  isExploded: false,     // Произошёл ли взрыв в BigBang
  reversedFactors: {     // Какие факторы включены в реверс
    acceleration: false,
    gravity: false,
    sun: false,
    // ...
  },
  nextStage: () => {},   // stage + 1 (макс. 4)
  prevStage: () => {},   // stage - 1 (мин. 0)
  toggleReverse: (key) => {} // Переключить реверс фактора
}
```

---

## Текущие баги (нужно исправить)

1. **Названия факторов не меняются при реверсе** — в `<Text>` компонентах Three.js используется статичный текст вместо динамического. Нужно везде заменить на:
   ```jsx
   // Было:
   <Text>УСКОРЕНИЕ</Text>
   // Должно быть:
   <Text>{reversedFactors['acceleration'] ? 'ЗАМЕДЛЕНИЕ' : 'УСКОРЕНИЕ'}</Text>
   ```
2. **Баг циклического повтора BigBang** — при скролле назад взрыв запускается повторно. Нужно проверить условие: взрыв должен срабатывать только один раз при `isExploded === false`.

---

## Что нужно реализовать (бэклог)

### 🔴 Критично (баги)

* [ ] Динамические названия факторов при реверсе во всех сценах
* [ ] Исправить баг повтора BigBang при скролле назад

### 🟠 Новые факторы (не реализованы)

* [ ] `Cosmos.jsx` — добавить фактор  **Солнце / Угасание** : пульсирующий `pointLight` + сфера, при реверсе свет тускнеет и сжимается
* [ ] `Cosmos.jsx` — добавить фактор  **Нагревание / Замерзание** : красно-оранжевые частицы от солнца, при реверсе замедляются и синеют
* [ ] `Planet.jsx` — добавить фактор  **Интерференция / Изоляция** : волновые кольца вокруг планеты, при реверсе сжимаются и гаснут
* [ ] `MicroCosmos.jsx` — добавить фактор  **Мутация / Стагнация** : деформирующийся `IcosahedronGeometry`, при реверсе замирает и серее

### 🟡 Визуальные улучшения

* [x] **Гравитация при распаде** — орбиты нарезаны на дуги (`OrbitRing` в
  `Cosmos.jsx`). При реверсе дуги отрываются одна за другой с собственным
  лагом, уходят наружу и кувыркаются; осколки ярче целой орбиты, потому что
  затухающие были почти не видны. Реверс обратно — дуги сходятся в кольцо.
* [x] Постобработка **Bloom** — собрана на родных проходах three
  (`scenes/effects/PostFX.jsx`). Пакет `@react-three/postprocessing` 2.16 здесь
  не работает: он импортирует `WebGLMultipleRenderTargets`, которого в three
  0.183 больше нет.
* [x] **Полярное сияние** на полюсах планеты
* [x] Финальный экран (Stage 6): шесть масштабов сходятся одной нитью света,
  по ней бежит импульс, вокруг вращаются искры по числу перевёрнутых факторов
* [x] **Модель тела (Stage 5)** — фигура собирается процедурно
  (`lib/anatomy.js`). `/model.gltf` больше не используется: 49 тысяч вершин, но
  силуэт деревянного манекена с прямоугольным торсом и палками вместо рук.
* [x] **Органы** собираются той же протяжкой сечений, что и фигура
  (`lib/organs.js`): сердце с верхушкой вниз-влево, доли лёгких с куполом,
  полушария мозга с продольной щелью, печень клином, желудок мешком, рамка
  толстой кишки и петли тонкой. Прежние сферы и буквальная пружина вместо
  кишечника убраны.

### Цветовой пайплайн (важно при правке шейдеров)

Кадр идёт через композер, а тональную компрессию и перевод в sRGB делает
`OutputPass`. Поэтому **свой шейдер должен отдавать линейный цвет** и
заканчиваться `#include <colorspace_fragment>` — чанк сам знает, куда идёт
рендер, и в буфере композера ничего не делает. Ручной `linearToSrgb` в конце
шейдера приводит к двойной гамме: планета выцветает, а подсолнечная точка
выгорает в белое пятно на четверть диска.

### 🟢 UX и оптимизация

* [x] Онбординг-подсказка при первом запуске (`components/Onboarding.jsx`):
  три строки в углу на макро-уровне, флаг в `localStorage`, кнопка «?» для
  повторного вызова. Не модальное окно: тур на полэкрана спорил бы с
  минимализмом слоёв.
* [ ] Проверить `InstancedMesh` для частиц, нейронов, звёзд
* [ ] Оптимизация для мобильных устройств
* [ ] Lazy loading сцен через `Suspense`

---

## Запуск проекта

```bash
cd Universe
npm install
npm run dev
```

### Проверки

Скрипты поднимают настоящий Chrome и ходят по слоям как пользователь.
Сначала должен быть запущен `npm run dev`.

```bash
node tests/transition-check.mjs   # все переходы + кадры вуали + FPS
node tests/scene-check.mjs        # FPS, draw calls и треугольники по слоям
node tests/journey-check.mjs      # пролёт космос → Земля → город
node tests/scroll-stages.mjs      # инерция колеса не проскакивает слой
```

Кадры ложатся в `tests/screenshots/`.

---

## Контекст и философия проекта

Проект создаётся как  **живая метафизическая карта реальности** . Это не энциклопедия и не лендинг — это цифровой опыт, где каждый слой раскрывает фундаментальные факторы, управляющие миром: от физики до сознания.

**Принципы дизайна:**

* Минимализм форм — никаких лишних иконок и декора
* Свет как главный элемент — связи это свет, активные узлы это пульсация
* Пустота как элемент — между объектами должно быть пространство
* Кинематографичность — все переходы медленные, как во сне

**Эмоция, которую должен испытывать пользователь:**

> "Я не просто изучаю вселенную. Я её активирую."

---

## Для ИИ-ассистента

При работе с этим проектом:

1. **Не ломай существующую архитектуру стадий** — навигация через Zustand store (`stage 0-4`) уже работает
2. **Все новые факторы** добавляй по единому паттерну: объект в сцене + запись в `FACTORS_DATA` в `App.jsx` + ключ в `reversedFactors` в `store.js`
3. **Динамические названия** — все `<Text>` компоненты должны читать из `reversedFactors`
4. **Стиль кода** — функциональные компоненты, хуки, без классов
5. **Производительность** — для больших массивов объектов (частицы, нейроны) использовать `InstancedMesh` или `BufferGeometry`
6. **Не отключай** существующие анимации GSAP при добавлении нового кода
7. 

### Что было сделано:

1. **Перевод на `InstancedMesh`:** Горы (18 шт), деревья (30 шт), здания цивилизации (50 шт), животные и птицы больше не рендерятся как отдельные объекты. Они объединены в высокопроизводительные инстансы, это драматически сократило количество Draw Calls (отрисовок на GPU) и радикально снизило лаги.
2. **Оптимизация `useFrame`:** Исключены дорогие вычисления `THREE.Quaternion` и создания `Vector3` на каждом кадре для сотен объектов одновременно.
3. **Фикс WebGL Crashes (Context Lost):** Браузер мог падать из-за утечки памяти в `useEarthTexture`, когда CanvasTexture создавалась заново при HMR (hot-reload) или других обновлениях без очистки. Я переписал этот хук с использованием `useState` + `useEffect` для безопасного управления ресурсами видеокарты.
4. **Освещение и цвета:** Исправлены баги с Invalid Hex Color, возвращен `AmbientLight` с увеличенной яркостью (иначе текстура планеты и `MeshStandardMaterial` выглядели как черные силуэты без света). И восстановлены случайно потертые в ходе масштабного рефакторинга компоненты атмосферы, океана, и интерференции.

Тесты браузерного агента подтвердили: лаги полностью пропали, WebGL больше не падает в черный экран. Можете проверить локально! (Если понадобится более яркая планета или изменение цветов, дайте знать)
