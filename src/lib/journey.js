import * as THREE from 'three';

/**
 * Киношный путь камеры: система → наезд на Землю.
 * Природа и город делят один ракурс: планета сама поворачивается к городам,
 * камера сквозь шар не летает.
 */
export const earthWorld = new THREE.Vector3(25, 0, 0);

export const SHOTS = {
    cosmos: { pos: [0, 13, 46], look: [0, 0, 0], fov: 55 },
    // Земля целиком в кадре сразу после пролёта из системы
    fromSpace: { pos: [0, 10, 38], look: [0, 0, 0], fov: 52 },
    // Портрет планеты целиком: диск, атмосфера и запас неба, без утыкания в поверхность
    earth: { pos: [0, 6.4, 32], look: [0, 0.4, 0], fov: 50 },
};
